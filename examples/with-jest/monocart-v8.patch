diff --git a/examples/with-jest/jest.config.js b/examples/with-jest/jest.config.js
index afde621b07..838ec1f265 100644
--- a/examples/with-jest/jest.config.js
+++ b/examples/with-jest/jest.config.js
@@ -9,6 +9,15 @@ const createJestConfig = nextJest({
 const customJestConfig = {
   setupFilesAfterEnv: ["<rootDir>/jest.setup.js"],
   testEnvironment: "jsdom",
+  collectCoverage: true,
+  coverageProvider: 'v8',
+  coverageDirectory: "./coverage/unit",
+  collectCoverageFrom: [
+      "app/**/*.{tsx,ts}",
+      "pages/**/*.{tsx,ts}",
+      "!dist"
+   ],
+  coverageReporters: ['lcov','text','text-summary'],
 };
 
 // createJestConfig is exported this way to ensure that next/jest can load the Next.js config which is async
diff --git a/examples/with-jest/package.json b/examples/with-jest/package.json
index e580311e33..6684c151c6 100644
--- a/examples/with-jest/package.json
+++ b/examples/with-jest/package.json
@@ -4,7 +4,8 @@
     "dev": "next dev",
     "build": "next build",
     "start": "next start",
-    "test": "jest --watch",
+    "test": "jest",
+    "test:monocart": "jest --config jest.config.monocart.js",
     "test:ci": "jest --ci"
   },
   "dependencies": {
@@ -20,6 +21,9 @@
     "@types/react": "18.2.45",
     "jest": "29.7.0",
     "jest-environment-jsdom": "29.7.0",
+    "jest-monocart-coverage": "^1.1.1",
+    "monocart-coverage-reports": "^2.10.3",
+    "monocart-reporter": "^2.8.0",
     "typescript": "5.3.3"
   }
 }
